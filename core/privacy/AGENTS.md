# Privacy Manifest

This directory owns the explicit local-first privacy manifest. User health data is device-owned by default in v1: subject profiles, observations, projections, predictions, and reports do not leave the device or persist on the server unless a future product decision changes that contract.

Keep this manifest conservative and readable by Flutter. It is a product contract, not legal boilerplate.
